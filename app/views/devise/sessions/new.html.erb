
<div class="row">
  <div class='span3'></div>
  <div class='span5' style='border: 1px solid;margin-top: 5%;box-shadow: 10px 10px 5px #777;padding: 1%;'>
    <h2 style="margin=20%; padding-bottom: 1%;text-align: center; border-bottom: 2px solid; border-bottom-color: #aaa; box-shadow: 0 7px 4px -4px lightblue; margin-bottom: 8%;">Sign in</h2>

    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), html: { class: 'form-horizontal'} ) do |f| %>
      <div class="control-group">
        <%= f.label :username, class: 'control-label' %>
        <div class="controls">
          <%= f.text_field :username, :autofocus => true, placeholder: 'Username' %>
        </div>
      </div>

      <div class="control-group">
        <%= f.label :passwor, class: 'control-label' %>
        <div class="controls">
          <%= f.password_field :password, placeholder: 'Password' %>
        </div>
      </div>

        <div class="control-group">
          <div class='controls'>
            <% if devise_mapping.rememberable? -%>
                <%= f.check_box :remember_me, class: 'checkbox' %>
                <%= f.label :remember_me %>
            <% end -%>

            <%= f.submit "Sign in", class: 'btn' %>
          </div>
        </div>

    <% end %>

    <div style="margin-left: 21%; margin-top: 5%;">
      <%= render "devise/shared/links" %>
    </div>

  </div>
</div>



<!--
        <form class="form-horizontal">
            <div class="control-group">
              <label class="control-label" for="inputEmail">Email</label>
              <div class="controls">
                <input type="text" id="inputEmail" placeholder="Email">
              </div>
            </div>
            <div class="control-group">
              <label class="control-label" for="inputPassword">Password</label>
              <div class="controls">
                <input type="password" id="inputPassword" placeholder="Password">
              </div>
            </div>
            <div class="control-group">
              <div class="controls">
                <label class="checkbox">
                  <input type="checkbox"> Remember me
                </label>
                <button type="submit" class="btn">Sign in</button>
              </div>
            </div>
          </form>
 -->
